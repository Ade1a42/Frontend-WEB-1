# lecture 1

# How internet works

1) First Client wants to get youtube.com and sends it address to ISP -> gets it and sends it to DNS -> DNS gets it and find its address that look like 216.58.210.46
2) DNS sends it back to ISP, ISP sends it to Internet backbone
```
Client -> ISP ( Internet Service Provider ) -> DNS Server

                              Internet backbone
                                    ^    ISP    <-  DNS
```

